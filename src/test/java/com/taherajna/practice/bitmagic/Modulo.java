package com.taherajna.practice.bitmagic;

import org.junit.Assert;
import org.junit.Test;

public class Modulo {

  @Test
  public void app() {
    Assert.assertEquals(true, 2 % 10 == 2);
    Assert.assertEquals(true, 20 % 100 == 20);
  }
}
